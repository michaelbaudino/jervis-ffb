package com.jervisffb.test.skills

/**
 * Class testing usage of the [com.jervisffb.engine.rules.bb2020.procedures.DivingCatch] skill
 */
class DivingCatchTests {
    // Diving Catch trigger during kick-off if ball lands next to player
    // Diving catch does not trigger if ball goes out of bounds during kick-off
    // Diving catch does not trigger if ball goes out of bounds during team turn
    // Diving catch does not trigger if ball goes over Line of Scrimmage
}

